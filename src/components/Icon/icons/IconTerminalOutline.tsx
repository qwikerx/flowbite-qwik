import { IconProps } from '@qwikest/icons'
import { FlTerminalOutline as QwikestIcon } from '@qwikest/icons/flowbite'
import { component$ } from '@builder.io/qwik'

export const IconTerminalOutline = component$((props: IconProps) => {
  return <QwikestIcon {...props} />
})